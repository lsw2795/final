<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ez.gw.secondhandTrade.model.SecondHandTradeDAO">
	<insert id="insertMarket" parameterType="secondHandTradeVO">
		<selectKey keyProperty="tradeNo" order="BEFORE" resultType="int">
			select secondhand_trade_seq.nextval from dual
		</selectKey>
		insert into secondhand_trade(trade_no, title, product, price, discription)
		values(#{tradeNo}, #{title}, #{product}, #{price}, #{discription})
	</insert>
	
	<select id="selectAllMarket" resultType="secondHandTradeVo">
	 select * from secondhand_trade 
	 <if test="searchKeyword!=null and searchKeyword!=''">
			where ${searchCondition} like '%' || #{searchKeyword} ||'%'
	</if>
	 order by regdate desc
	</select>
	
	<select id="selectMarketByNo" parameterType="int" resultType="secondHandTradeVo">
		select * from secondhand_trade 
		where trade_no = #{tradeNo}
	</select>
	
	<update id="updateReadCount" parameterType="int" >
		update secondhand_trade set readcount = readCount + 1
		where trade_no = #{tradeNo}
	</update>
	
	<select id="getTotalRecord" parameterType="secondHandTradeVo" resultType="int">
		 select count(*)
    	from secondhand_trade
    <where>
        <if test="searchKeyword!=null and searchKeyword!=''">
            <choose>
                <when test="searchCondition == 'total'">
                    title like '%' || #{searchKeyword} || '%' or discription like '%' || #{searchKeyword} || '%'
                </when>
                <otherwise>
                    ${searchCondition} like '%' || #{searchKeyword} || '%'
                </otherwise>
            </choose>
        </if>
    </where>
	</select>
</mapper>