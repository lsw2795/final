<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ez.gw.secondhandTrade.model.SecondHandTradeDAO">
	<insert id="insertMarket" parameterType="secondHandTradeVO">
		<selectKey keyProperty="tradeNo" order="BEFORE" resultType="int">
			select secondhand_trade_seq.nextval from dual
		</selectKey>
		insert into secondhand_trade(trade_no, title, product, price, discription, mainImgURL)
		values(#{tradeNo}, #{title}, #{product}, #{price}, #{discription}, #{mainImgURL})
	</insert>
	
	<insert id="insertFile" parameterType="secondHandTradeFileVo" >
		<selectKey keyProperty="fileNo" order="BEFORE" resultType="int">
			select secondhand_trade_file_seq.nextval from dual
		</selectKey>
		insert into secondhand_trade_file(fileNo, originalFilename)
		values(#{fileNo}, #{originalFileName})
	</insert>

</mapper>