<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ez.gw.clubboardComment.model.ClubBoardCommentDAO">
	
	<insert id="insertClubComm">
		<selectKey keyProperty="clubCommNo" order="BEFORE" resultType="int">
			select CLUBBOARD_COMMENT_seq.nextval from dual
		</selectKey>
			insert into CLUBBOARD_COMMENT(COMMENT_NO, CONTENT, GROUPNO, STEP, SORTNO,
    			EMP_NO, BOARD_NO)
			values(#{clubCommNo},#{content},#{groupNo},#{step},#{sortNo},#{empNo},#{boardNo})
	</insert>
	
	<select id="selectCommClub">
		
	</select>
	
</mapper>