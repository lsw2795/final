<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ez.gw.commute.model.CommuteDAO">

	<select id="selectCommuteByEmpNo" parameterType="int" resultType="commuteVo">
		select * from commute
		where emp_no=#{empNo}
	</select>
	
	<insert id="insertWorkIn" parameterType="int">
	    insert into commute(commute_no, work_in, commute_state, emp_no)
	    values(commute_seq.nextval, to_char(sysdate, 'yyyy-mm-dd hh24:mi:ss'), 0, #{empNo})
	</insert>
	
	<update id="updateWorkOut" parameterType="int">
	    update commute
	    set work_out = to_char(sysdate, 'yyyy-mm-dd hh24:mi:ss')
	    where emp_no=#{empNo} and work_in is not null
	</update>
	
	<select id="selectIsWorkIn" parameterType="int" resultType="int">
		select count(*)
		from commute
		where work_in like '%' || to_char(sysdate, 'yyyy-mm-dd') || '%' and emp_no = #{empNo}
	</select>
	
	<select id="selectIsWorkOut" parameterType="int" resultType="int">
		select count(*)
		from commute
		where work_out like '%' || to_char(sysdate, 'yyyy-mm-dd') || '%' and emp_no = #{empNo}
	</select>
	
</mapper>